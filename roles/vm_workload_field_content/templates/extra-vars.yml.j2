---
guid: "{{ guid }}"
field_asset:
  node_count: {{ node_instance_count | default(1) | int }}
  node_size: "{{ node_size | default('medium') }}"
  rhel_version: "{{ rhel_version | default('rhel9') }}"
